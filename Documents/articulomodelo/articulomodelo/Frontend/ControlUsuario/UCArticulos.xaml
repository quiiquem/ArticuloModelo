<UserControl x:Class="articulomodelo.Frontend.ControlUsuario.UCArticulos"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:articulomodelo.Frontend.ControlUsuario"
        mc:Ignorable="d"
        d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        
        
        <!--Mostrar más datos que los principales-->
        
        <!--<DataGrid.RowDetailsTemplate>
            <DataTemplate>
                <DockPanel>
                    <Grid DockPanel.Dock="Top">
                        <TextBlock Text="LISTADO DE cosas del AM no se me ocurre nada rn"
                           FontSize="14" FontWeight="Bold" Margin="10"/>
                    </Grid>
                    <Grid>
                        <ListBox ItemsSource="{Binding Salida}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="Gray"
                                    BorderThickness="2"
                                    Margin="5"
                                    Padding="5"
                                    >
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </DockPanel>
            </DataTemplate>
        </DataGrid.RowDetailsTemplate>-->
    </Grid>
</UserControl>
